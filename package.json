{
  "name": "cocobackend",
  "version": "1.0.0",
  "description": "json server for coco",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 8080"
  },
  "author": "Roshan Shibu",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.2"
  }
}
